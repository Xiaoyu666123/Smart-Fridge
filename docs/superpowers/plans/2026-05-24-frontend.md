# 智能冰箱前端实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 为智能冰箱食材管理系统构建 Vue 3 + Element Plus 前端，包含库存概览、AI 对话、食材识别、偏好设置等页面，支持主题切换。

**Architecture:** Vite 构建，Vue Router 路由，Pinia 状态管理，Axios 调用后端 API，Element Plus 组件库。

**Tech Stack:** Vue 3, TypeScript, Element Plus, Vue Router, Pinia, Axios, Vite

---

## 文件清单

| 操作 | 文件 | 职责 |
|------|------|------|
| 新建 | `web/package.json` | 项目依赖 |
| 新建 | `web/vite.config.ts` | Vite 配置（代理、别名） |
| 新建 | `web/tsconfig.json` | TypeScript 配置 |
| 新建 | `web/tsconfig.node.json` | Node TypeScript 配置 |
| 新建 | `web/index.html` | 入口 HTML |
| 新建 | `web/src/main.ts` | 应用入口 |
| 新建 | `web/src/App.vue` | 根组件 |
| 新建 | `web/src/style.css` | 全局样式 + CSS 变量 |
| 新建 | `web/src/env.d.ts` | 类型声明 |
| 新建 | `web/src/api/index.ts` | Axios 实例 |
| 新建 | `web/src/api/inventory.ts` | 库存 API |
| 新建 | `web/src/api/agent.ts` | Agent API |
| 新建 | `web/src/api/event.ts` | 事件 API |
| 新建 | `web/src/stores/theme.ts` | 主题 store |
| 新建 | `web/src/stores/chat.ts` | 对话 store |
| 新建 | `web/src/router/index.ts` | 路由配置 |
| 新建 | `web/src/components/Layout.vue` | 布局组件 |
| 新建 | `web/src/components/ChatMessage.vue` | 聊天气泡 |
| 新建 | `web/src/components/FoodCard.vue` | 食材卡片 |
| 新建 | `web/src/views/Inventory.vue` | 库存概览 |
| 新建 | `web/src/views/Chat.vue` | AI 食谱推荐 |
| 新建 | `web/src/views/Recognize.vue` | 食材识别 |
| 新建 | `web/src/views/Preferences.vue` | 偏好设置 |
| 新建 | `web/src/views/AgentConfig.vue` | Agent 管理（占位） |
| 新建 | `web/src/views/Workflow.vue` | 工作流管理（占位） |
| 新建 | `web/src/views/Logs.vue` | 系统日志（占位） |

---

### Task 1: 项目初始化

**Files:**
- New: `web/package.json`
- New: `web/vite.config.ts`
- New: `web/tsconfig.json`
- New: `web/tsconfig.node.json`
- New: `web/index.html`
- New: `web/src/env.d.ts`

- [ ] **Step 1: 创建 package.json**

```json
{
  "name": "fridge-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vue": "^3.5.13",
    "vue-router": "^4.5.0",
    "pinia": "^2.3.0",
    "axios": "^1.7.9",
    "element-plus": "^2.9.1",
    "@element-plus/icons-vue": "^2.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.1",
    "typescript": "~5.6.3",
    "vite": "^6.0.5",
    "vue-tsc": "^2.2.0"
  }
}
```

- [ ] **Step 2: 创建 vite.config.ts**

```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import path from 'path'

export default defineConfig({
  plugins: [vue()],
  resolve: {
    alias: {
      '@': path.resolve(__dirname, 'src'),
    },
  },
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8000',
        changeOrigin: true,
      },
    },
  },
})
```

- [ ] **Step 3: 创建 tsconfig.json**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "preserve",
    "strict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true,
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["src/**/*.ts", "src/**/*.tsx", "src/**/*.vue"],
  "references": [{ "path": "./tsconfig.node.json" }]
}
```

- [ ] **Step 4: 创建 tsconfig.node.json**

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "lib": ["ES2023"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "noEmit": true,
    "strict": true,
    "noUnusedLocals": false,
    "noUnusedParameters": false,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["vite.config.ts"]
}
```

- [ ] **Step 5: 创建 index.html**

```html
<!DOCTYPE html>
<html lang="zh-CN">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>智能冰箱食材管理系统</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

- [ ] **Step 6: 创建 src/env.d.ts**

```ts
/// <reference types="vite/client" />

declare module '*.vue' {
  import type { DefineComponent } from 'vue'
  const component: DefineComponent<{}, {}, any>
  export default component
}
```

- [ ] **Step 7: 安装依赖**

```bash
cd C:/Users/86134/PycharmProjects/luckfox_web/web && npm install
```

---

### Task 2: 全局样式与主题

**Files:**
- New: `web/src/style.css`

- [ ] **Step 1: 创建 style.css**

```css
:root {
  --bg-color: #f5f7fa;
  --bg-card: #ffffff;
  --text-primary: #303133;
  --text-secondary: #606266;
  --text-placeholder: #a8abb2;
  --border-color: #dcdfe6;
  --sidebar-bg: #001529;
  --sidebar-text: #ffffff;
  --sidebar-active: #409eff;
}

html.dark {
  --bg-color: #141414;
  --bg-card: #1d1e1f;
  --text-primary: #e5eaf3;
  --text-secondary: #a3a6ad;
  --text-placeholder: #6b6d72;
  --border-color: #414243;
  --sidebar-bg: #0d0d0d;
  --sidebar-text: #e5eaf3;
  --sidebar-active: #409eff;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Helvetica Neue', Helvetica, 'PingFang SC', 'Hiragino Sans GB',
    'Microsoft YaHei', '微软雅黑', Arial, sans-serif;
  background-color: var(--bg-color);
  color: var(--text-primary);
  transition: background-color 0.3s, color 0.3s;
}

#app {
  width: 100%;
  height: 100vh;
}
```

---

### Task 3: API 层

**Files:**
- New: `web/src/api/index.ts`
- New: `web/src/api/inventory.ts`
- New: `web/src/api/agent.ts`
- New: `web/src/api/event.ts`

- [ ] **Step 1: 创建 api/index.ts**

```ts
import axios from 'axios'

const api = axios.create({
  baseURL: '/api/v1',
  timeout: 30000,
})

api.interceptors.response.use(
  (response) => response.data,
  (error) => {
    const msg = error.response?.data?.detail || error.message || '请求失败'
    console.error('API Error:', msg)
    return Promise.reject(error)
  }
)

export default api
```

- [ ] **Step 2: 创建 api/inventory.ts**

```ts
import api from './index'

export interface InventoryItem {
  id: string
  device_id: string
  category: string
  status: string
  remain_ratio: number
  bbox: number[] | null
  agent_metadata: Record<string, any> | null
  created_at: string | null
  update_at: string | null
  stored_at: string | null
}

export function getInventoryList(params?: { device_id?: string; status?: string }) {
  return api.get<any, InventoryItem[]>('/inventory', { params })
}
```

- [ ] **Step 3: 创建 api/agent.ts**

```ts
import api from './index'

export interface ChatRequest {
  user_id: string
  message: string
  city?: string
}

export interface ChatResponse {
  reply: string
  detected_preferences: Array<{ key: string; value: string }>
}

export interface RecognizeResponse {
  category: string
  confidence: number
  shelf_life_days: number
  storage_advice: string
}

export interface PreferenceItem {
  id: string
  user_id: string
  preference_key: string
  preference_value: string
  source: string | null
  created_at: string | null
}

export function chat(data: ChatRequest) {
  return api.post<any, ChatResponse>('/agent/chat', data)
}

export function recognize(data: { image: string }) {
  return api.post<any, RecognizeResponse>('/agent/recognize', data)
}

export function getPreferences(userId: string) {
  return api.get<any, PreferenceItem[]>('/agent/preferences', { params: { user_id: userId } })
}
```

- [ ] **Step 4: 创建 api/event.ts**

```ts
import api from './index'

export interface EventLog {
  id: number
  inventory_id: string
  event_type: string
  confidence: number | null
  snapshot_path: string | null
  create_at: string | null
}

export function getEventLogs(params?: { inventory_id?: string }) {
  return api.get<any, EventLog[]>('/events', { params })
}
```

---

### Task 4: 状态管理

**Files:**
- New: `web/src/stores/theme.ts`
- New: `web/src/stores/chat.ts`

- [ ] **Step 1: 创建 stores/theme.ts**

```ts
import { defineStore } from 'pinia'
import { ref, watch } from 'vue'

export const useThemeStore = defineStore('theme', () => {
  const isDark = ref(localStorage.getItem('theme') === 'dark')

  function toggle() {
    isDark.value = !isDark.value
  }

  watch(isDark, (val) => {
    localStorage.setItem('theme', val ? 'dark' : 'light')
    document.documentElement.classList.toggle('dark', val)
  }, { immediate: true })

  return { isDark, toggle }
})
```

- [ ] **Step 2: 创建 stores/chat.ts**

```ts
import { defineStore } from 'pinia'
import { ref } from 'vue'

export interface Message {
  role: 'user' | 'assistant'
  content: string
  preferences?: Array<{ key: string; value: string }>
}

export const useChatStore = defineStore('chat', () => {
  const userId = ref('user_001')
  const messages = ref<Message[]>([])
  const loading = ref(false)

  function addMessage(msg: Message) {
    messages.value.push(msg)
  }

  function clearMessages() {
    messages.value = []
  }

  return { userId, messages, loading, addMessage, clearMessages }
})
```

---

### Task 5: 路由配置

**Files:**
- New: `web/src/router/index.ts`

- [ ] **Step 1: 创建 router/index.ts**

```ts
import { createRouter, createWebHistory } from 'vue-router'

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/',
      name: 'Inventory',
      component: () => import('@/views/Inventory.vue'),
      meta: { title: '库存概览' },
    },
    {
      path: '/chat',
      name: 'Chat',
      component: () => import('@/views/Chat.vue'),
      meta: { title: 'AI 食谱推荐' },
    },
    {
      path: '/recognize',
      name: 'Recognize',
      component: () => import('@/views/Recognize.vue'),
      meta: { title: '食材识别' },
    },
    {
      path: '/preferences',
      name: 'Preferences',
      component: () => import('@/views/Preferences.vue'),
      meta: { title: '偏好设置' },
    },
    {
      path: '/agent',
      name: 'AgentConfig',
      component: () => import('@/views/AgentConfig.vue'),
      meta: { title: 'Agent 管理' },
    },
    {
      path: '/workflow',
      name: 'Workflow',
      component: () => import('@/views/Workflow.vue'),
      meta: { title: '工作流管理' },
    },
    {
      path: '/logs',
      name: 'Logs',
      component: () => import('@/views/Logs.vue'),
      meta: { title: '系统日志' },
    },
  ],
})

export default router
```

---

### Task 6: 入口文件与根组件

**Files:**
- New: `web/src/main.ts`
- New: `web/src/App.vue`

- [ ] **Step 1: 创建 src/main.ts**

```ts
import { createApp } from 'vue'
import { createPinia } from 'pinia'
import ElementPlus from 'element-plus'
import 'element-plus/dist/index.css'
import * as ElementPlusIconsVue from '@element-plus/icons-vue'

import App from './App.vue'
import router from './router'
import './style.css'

const app = createApp(App)

app.use(createPinia())
app.use(router)
app.use(ElementPlus)

for (const [key, component] of Object.entries(ElementPlusIconsVue)) {
  app.component(key, component)
}

app.mount('#app')
```

- [ ] **Step 2: 创建 src/App.vue**

```vue
<script setup lang="ts">
import Layout from './components/Layout.vue'
</script>

<template>
  <Layout />
</template>

<style scoped>
</style>
```

---

### Task 7: 布局组件

**Files:**
- New: `web/src/components/Layout.vue`

- [ ] **Step 1: 创建 Layout.vue**

```vue
<script setup lang="ts">
import { ref, computed } from 'vue'
import { useRouter, useRoute } from 'vue-router'
import { useThemeStore } from '@/stores/theme'

const router = useRouter()
const route = useRoute()
const themeStore = useThemeStore()
const isCollapse = ref(false)

const menuItems = [
  { path: '/', icon: 'Box', title: '库存概览' },
  { path: '/chat', icon: 'ChatDotRound', title: 'AI 食谱推荐' },
  { path: '/recognize', icon: 'Camera', title: '食材识别' },
  { path: '/preferences', icon: 'Setting', title: '偏好设置' },
  { path: '/agent', icon: 'Cpu', title: 'Agent 管理' },
  { path: '/workflow', icon: 'Connection', title: '工作流管理' },
  { path: '/logs', icon: 'Document', title: '系统日志' },
]

const pageTitle = computed(() => route.meta.title || '智能冰箱')

function handleMenuSelect(path: string) {
  router.push(path)
}
</script>

<template>
  <el-container style="height: 100vh">
    <el-aside :width="isCollapse ? '64px' : '220px'" style="transition: width 0.3s; background: var(--sidebar-bg)">
      <div style="height: 60px; display: flex; align-items: center; justify-content: center; color: var(--sidebar-text); font-size: 18px; font-weight: bold; white-space: nowrap; overflow: hidden">
        <span v-if="!isCollapse">智能冰箱</span>
        <span v-else>🧊</span>
      </div>
      <el-menu
        :default-active="route.path"
        :collapse="isCollapse"
        background-color="transparent"
        text-color="var(--sidebar-text)"
        active-text-color="var(--sidebar-active)"
        @select="handleMenuSelect"
      >
        <el-menu-item v-for="item in menuItems" :key="item.path" :index="item.path">
          <el-icon><component :is="item.icon" /></el-icon>
          <template #title>{{ item.title }}</template>
        </el-menu-item>
      </el-menu>
    </el-aside>

    <el-container>
      <el-header style="display: flex; align-items: center; justify-content: space-between; border-bottom: 1px solid var(--border-color); background: var(--bg-card)">
        <div style="display: flex; align-items: center; gap: 12px">
          <el-icon style="cursor: pointer; font-size: 20px" @click="isCollapse = !isCollapse">
            <Fold v-if="!isCollapse" />
            <Expand v-else />
          </el-icon>
          <span style="font-size: 16px; font-weight: 600">{{ pageTitle }}</span>
        </div>
        <el-switch
          v-model="themeStore.isDark"
          inline-prompt
          :active-icon="'Moon'"
          :inactive-icon="'Sunny'"
          @change="themeStore.toggle()"
        />
      </el-header>

      <el-main style="padding: 20px; background: var(--bg-color)">
        <router-view />
      </el-main>
    </el-container>
  </el-container>
</template>

<style scoped>
.el-menu {
  border-right: none;
}
</style>
```

---

### Task 8: 公共组件

**Files:**
- New: `web/src/components/ChatMessage.vue`
- New: `web/src/components/FoodCard.vue`

- [ ] **Step 1: 创建 ChatMessage.vue**

```vue
<script setup lang="ts">
defineProps<{
  role: 'user' | 'assistant'
  content: string
}>()
</script>

<template>
  <div :class="['message', role]">
    <div class="avatar">
      <el-icon v-if="role === 'user'" :size="20"><User /></el-icon>
      <el-icon v-else :size="20"><Cpu /></el-icon>
    </div>
    <div class="bubble">
      <div class="content">{{ content }}</div>
    </div>
  </div>
</template>

<style scoped>
.message {
  display: flex;
  gap: 12px;
  margin-bottom: 16px;
}
.message.user {
  flex-direction: row-reverse;
}
.avatar {
  width: 36px;
  height: 36px;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  flex-shrink: 0;
}
.message.user .avatar {
  background: #409eff;
  color: white;
}
.message.assistant .avatar {
  background: #67c23a;
  color: white;
}
.bubble {
  max-width: 70%;
  padding: 12px 16px;
  border-radius: 12px;
  line-height: 1.6;
  white-space: pre-wrap;
}
.message.user .bubble {
  background: #409eff;
  color: white;
  border-top-right-radius: 4px;
}
.message.assistant .bubble {
  background: var(--bg-card);
  color: var(--text-primary);
  border: 1px solid var(--border-color);
  border-top-left-radius: 4px;
}
</style>
```

- [ ] **Step 2: 创建 FoodCard.vue**

```vue
<script setup lang="ts">
defineProps<{
  category: string
  confidence: number
  shelfLifeDays: number
  storageAdvice: string
}>()
</script>

<template>
  <el-card shadow="hover">
    <template #header>
      <div style="display: flex; align-items: center; gap: 8px">
        <el-icon :size="24" color="#409eff"><Food /></el-icon>
        <span style="font-size: 18px; font-weight: 600">{{ category }}</span>
      </div>
    </template>
    <el-descriptions :column="1" border>
      <el-descriptions-item label="置信度">
        <el-progress :percentage="Math.round(confidence * 100)" :color="confidence > 0.8 ? '#67c23a' : '#e6a23c'" />
      </el-descriptions-item>
      <el-descriptions-item label="保鲜天数">
        <el-tag :type="shelfLifeDays > 3 ? 'success' : shelfLifeDays > 1 ? 'warning' : 'danger'">
          {{ shelfLifeDays }} 天
        </el-tag>
      </el-descriptions-item>
      <el-descriptions-item label="存储建议">
        {{ storageAdvice }}
      </el-descriptions-item>
    </el-descriptions>
  </el-card>
</template>
```

---

### Task 9: 库存概览页面

**Files:**
- New: `web/src/views/Inventory.vue`

- [ ] **Step 1: 创建 Inventory.vue**

```vue
<script setup lang="ts">
import { ref, onMounted, computed } from 'vue'
import { getInventoryList, type InventoryItem } from '@/api/inventory'

const loading = ref(false)
const items = ref<InventoryItem[]>([])
const filterDeviceId = ref('')
const filterStatus = ref('')

async function fetchInventory() {
  loading.value = true
  try {
    const params: Record<string, string> = {}
    if (filterDeviceId.value) params.device_id = filterDeviceId.value
    if (filterStatus.value) params.status = filterStatus.value
    items.value = await getInventoryList(params)
  } catch (e) {
    console.error(e)
  } finally {
    loading.value = false
  }
}

function getRemainDays(item: InventoryItem): number {
  const metadata = item.agent_metadata
  if (!metadata?.expire_at) return -1
  const expire = new Date(metadata.expire_at)
  const now = new Date()
  return Math.max(0, Math.ceil((expire.getTime() - now.getTime()) / (1000 * 60 * 60 * 24)))
}

function getStatusType(item: InventoryItem): string {
  const days = getRemainDays(item)
  if (days < 0) return 'info'
  if (days <= 1) return 'danger'
  if (days <= 3) return 'warning'
  return 'success'
}

function formatDate(dateStr: string | null): string {
  if (!dateStr) return '-'
  return new Date(dateStr).toLocaleString('zh-CN')
}

onMounted(fetchInventory)
</script>

<template>
  <div>
    <el-card shadow="never" style="margin-bottom: 16px">
      <el-form :inline="true">
        <el-form-item label="设备ID">
          <el-input v-model="filterDeviceId" placeholder="输入设备ID" clearable />
        </el-form-item>
        <el-form-item label="状态">
          <el-select v-model="filterStatus" placeholder="选择状态" clearable>
            <el-option label="在库" value="IN_STOCK" />
            <el-option label="待确认" value="OUT_PENDING" />
            <el-option label="已消耗" value="CONSUMED" />
          </el-select>
        </el-form-item>
        <el-form-item>
          <el-button type="primary" @click="fetchInventory">查询</el-button>
        </el-form-item>
      </el-form>
    </el-card>

    <el-card shadow="never">
      <el-table :data="items" v-loading="loading" stripe>
        <el-table-column prop="category" label="食材名称" width="120" />
        <el-table-column prop="device_id" label="设备ID" width="180" />
        <el-table-column prop="status" label="状态" width="100">
          <template #default="{ row }">
            <el-tag :type="row.status === 'IN_STOCK' ? 'success' : row.status === 'OUT_PENDING' ? 'warning' : 'info'">
              {{ row.status === 'IN_STOCK' ? '在库' : row.status === 'OUT_PENDING' ? '待确认' : '已消耗' }}
            </el-tag>
          </template>
        </el-table-column>
        <el-table-column label="保鲜状态" width="120">
          <template #default="{ row }">
            <el-tag :type="getStatusType(row)">
              {{ getRemainDays(row) >= 0 ? `${getRemainDays(row)} 天` : '未知' }}
            </el-tag>
          </template>
        </el-table-column>
        <el-table-column label="入库时间" width="180">
          <template #default="{ row }">{{ formatDate(row.created_at) }}</template>
        </el-table-column>
        <el-table-column label="过期时间" width="180">
          <template #default="{ row }">{{ formatDate(row.agent_metadata?.expire_at) }}</template>
        </el-table-column>
        <el-table-column prop="remain_ratio" label="剩余比例" width="100">
          <template #default="{ row }">
            <el-progress :percentage="Math.round(row.remain_ratio * 100)" :stroke-width="8" />
          </template>
        </el-table-column>
      </el-table>
    </el-card>
  </div>
</template>
```

---

### Task 10: AI 食谱推荐页面

**Files:**
- New: `web/src/views/Chat.vue`

- [ ] **Step 1: 创建 Chat.vue**

```vue
<script setup lang="ts">
import { ref, nextTick } from 'vue'
import { ElMessage } from 'element-plus'
import { useChatStore } from '@/stores/chat'
import { chat } from '@/api/agent'
import ChatMessage from '@/components/ChatMessage.vue'

const chatStore = useChatStore()
const inputMessage = ref('')
const chatBody = ref<HTMLElement>()
const city = ref('')

function scrollToBottom() {
  nextTick(() => {
    if (chatBody.value) {
      chatBody.value.scrollTop = chatBody.value.scrollHeight
    }
  })
}

async function sendMessage() {
  const msg = inputMessage.value.trim()
  if (!msg) return

  chatStore.addMessage({ role: 'user', content: msg })
  inputMessage.value = ''
  chatStore.loading = true
  scrollToBottom()

  try {
    const res = await chat({
      user_id: chatStore.userId,
      message: msg,
      city: city.value || undefined,
    })
    chatStore.addMessage({
      role: 'assistant',
      content: res.reply,
      preferences: res.detected_preferences,
    })

    if (res.detected_preferences.length > 0) {
      const prefs = res.detected_preferences.map(p => p.value).join('、')
      ElMessage.success(`已记住您的偏好：${prefs}`)
    }
  } catch (e) {
    chatStore.addMessage({ role: 'assistant', content: '抱歉，推荐服务暂时不可用，请稍后再试。' })
  } finally {
    chatStore.loading = false
    scrollToBottom()
  }
}

function handleKeydown(e: KeyboardEvent) {
  if (e.key === 'Enter' && !e.shiftKey) {
    e.preventDefault()
    sendMessage()
  }
}
</script>

<template>
  <el-card shadow="never" style="height: calc(100vh - 150px); display: flex; flex-direction: column">
    <template #header>
      <div style="display: flex; align-items: center; justify-content: space-between">
        <span style="font-size: 16px; font-weight: 600">AI 食谱推荐</span>
        <div style="display: flex; gap: 12px; align-items: center">
          <el-input v-model="city" placeholder="城市（可选）" style="width: 120px" size="small" clearable />
          <el-input v-model="chatStore.userId" placeholder="用户ID" style="width: 120px" size="small" />
          <el-button size="small" @click="chatStore.clearMessages()">清空对话</el-button>
        </div>
      </div>
    </template>

    <div ref="chatBody" style="flex: 1; overflow-y: auto; padding: 16px; background: var(--bg-color); border-radius: 8px">
      <div v-if="chatStore.messages.length === 0" style="text-align: center; color: var(--text-placeholder); padding-top: 100px">
        <el-icon :size="48"><ChatDotRound /></el-icon>
        <p style="margin-top: 12px">告诉我你想吃什么，我来推荐食谱</p>
      </div>
      <ChatMessage v-for="(msg, i) in chatStore.messages" :key="i" :role="msg.role" :content="msg.content" />
      <div v-if="chatStore.loading" style="display: flex; gap: 12px; margin-bottom: 16px">
        <div style="width: 36px; height: 36px; border-radius: 50%; background: #67c23a; display: flex; align-items: center; justify-content: center; color: white">
          <el-icon :size="20"><Cpu /></el-icon>
        </div>
        <div style="padding: 12px 16px; background: var(--bg-card); border-radius: 12px; border: 1px solid var(--border-color)">
          <el-icon class="is-loading"><Loading /></el-icon> 思考中...
        </div>
      </div>
    </div>

    <div style="margin-top: 16px; display: flex; gap: 12px">
      <el-input
        v-model="inputMessage"
        type="textarea"
        :rows="2"
        placeholder="输入消息，按 Enter 发送..."
        @keydown="handleKeydown"
        style="flex: 1"
      />
      <el-button type="primary" :loading="chatStore.loading" @click="sendMessage" style="align-self: flex-end">
        发送
      </el-button>
    </div>
  </el-card>
</template>
```

---

### Task 11: 食材识别页面

**Files:**
- New: `web/src/views/Recognize.vue`

- [ ] **Step 1: 创建 Recognize.vue**

```vue
<script setup lang="ts">
import { ref } from 'vue'
import { ElMessage } from 'element-plus'
import { recognize, type RecognizeResponse } from '@/api/agent'
import FoodCard from '@/components/FoodCard.vue'

const loading = ref(false)
const result = ref<RecognizeResponse | null>(null)
const imageUrl = ref('')

function handleUpload(uploadFile: any) {
  const file = uploadFile.raw
  if (!file) return

  if (file.size > 5 * 1024 * 1024) {
    ElMessage.warning('图片大小不能超过 5MB')
    return
  }

  imageUrl.value = URL.createObjectURL(file)

  const reader = new FileReader()
  reader.onload = async (e) => {
    const base64 = (e.target?.result as string).split(',')[1]
    loading.value = true
    try {
      result.value = await recognize({ image: base64 })
    } catch (err) {
      ElMessage.error('识别失败，请重试')
    } finally {
      loading.value = false
    }
  }
  reader.readAsDataURL(file)
}
</script>

<template>
  <div style="display: flex; gap: 20px">
    <el-card shadow="never" style="flex: 1">
      <template #header>
        <span style="font-size: 16px; font-weight: 600">上传图片</span>
      </template>
      <el-upload
        drag
        :auto-upload="false"
        :show-file-list="false"
        accept="image/*"
        @change="handleUpload"
        v-loading="loading"
      >
        <img v-if="imageUrl" :src="imageUrl" style="max-width: 100%; max-height: 300px; object-fit: contain" />
        <div v-else>
          <el-icon :size="48" color="#909399"><UploadFilled /></el-icon>
          <div style="margin-top: 8px; color: var(--text-secondary)">拖拽图片到此处，或点击上传</div>
          <div style="margin-top: 4px; color: var(--text-placeholder); font-size: 12px">支持 JPG/PNG，最大 5MB</div>
        </div>
      </el-upload>
    </el-card>

    <el-card shadow="never" style="flex: 1">
      <template #header>
        <span style="font-size: 16px; font-weight: 600">识别结果</span>
      </template>
      <div v-if="result" style="display: flex; flex-direction: column; gap: 16px">
        <FoodCard
          :category="result.category"
          :confidence="result.confidence"
          :shelf-life-days="result.shelf_life_days"
          :storage-advice="result.storage_advice"
        />
      </div>
      <div v-else style="text-align: center; color: var(--text-placeholder); padding: 60px 0">
        <el-icon :size="48"><Camera /></el-icon>
        <p style="margin-top: 12px">上传图片后查看识别结果</p>
      </div>
    </el-card>
  </div>
</template>
```

---

### Task 12: 偏好设置页面

**Files:**
- New: `web/src/views/Preferences.vue`

- [ ] **Step 1: 创建 Preferences.vue**

```vue
<script setup lang="ts">
import { ref, onMounted } from 'vue'
import { ElMessage } from 'element-plus'
import { getPreferences, type PreferenceItem } from '@/api/agent'

const loading = ref(false)
const items = ref<PreferenceItem[]>([])
const userId = ref('user_001')
const showAddDialog = ref(false)
const newPref = ref({ key: 'taste', value: '' })

const prefTypes = [
  { label: '口味', value: 'taste' },
  { label: '过敏', value: 'allergy' },
  { label: '忌口', value: 'dislike' },
  { label: '饮食方式', value: 'prefer' },
]

async function fetchPreferences() {
  loading.value = true
  try {
    items.value = await getPreferences(userId.value)
  } catch (e) {
    console.error(e)
  } finally {
    loading.value = false
  }
}

function handleAdd() {
  if (!newPref.value.value.trim()) {
    ElMessage.warning('请输入偏好值')
    return
  }
  items.value.push({
    id: Date.now().toString(),
    user_id: userId.value,
    preference_key: newPref.value.key,
    preference_value: newPref.value.value,
    source: 'manual',
    created_at: new Date().toISOString(),
  })
  showAddDialog.value = false
  newPref.value = { key: 'taste', value: '' }
  ElMessage.success('添加成功（本地）')
}

function handleDelete(index: number) {
  items.value.splice(index, 1)
  ElMessage.success('删除成功（本地）')
}

function getPrefTypeLabel(key: string): string {
  return prefTypes.find(t => t.value === key)?.label || key
}

onMounted(fetchPreferences)
</script>

<template>
  <el-card shadow="never">
    <template #header>
      <div style="display: flex; align-items: center; justify-content: space-between">
        <div style="display: flex; align-items: center; gap: 12px">
          <span style="font-size: 16px; font-weight: 600">偏好设置</span>
          <el-input v-model="userId" placeholder="用户ID" style="width: 150px" size="small" />
          <el-button size="small" type="primary" @click="fetchPreferences">查询</el-button>
        </div>
        <el-button type="primary" @click="showAddDialog = true">
          <el-icon><Plus /></el-icon> 添加偏好
        </el-button>
      </div>
    </template>

    <el-table :data="items" v-loading="loading" stripe>
      <el-table-column label="偏好类型" width="120">
        <template #default="{ row }">
          <el-tag>{{ getPrefTypeLabel(row.preference_key) }}</el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="preference_value" label="偏好值" />
      <el-table-column prop="source" label="来源" width="100">
        <template #default="{ row }">
          <el-tag :type="row.source === 'chat' ? 'success' : 'info'">
            {{ row.source === 'chat' ? '对话学习' : '手动设置' }}
          </el-tag>
        </template>
      </el-table-column>
      <el-table-column label="创建时间" width="180">
        <template #default="{ row }">
          {{ row.created_at ? new Date(row.created_at).toLocaleString('zh-CN') : '-' }}
        </template>
      </el-table-column>
      <el-table-column label="操作" width="80">
        <template #default="{ $index }">
          <el-button type="danger" size="small" text @click="handleDelete($index)">删除</el-button>
        </template>
      </el-table-column>
    </el-table>

    <el-dialog v-model="showAddDialog" title="添加偏好" width="400px">
      <el-form label-width="80px">
        <el-form-item label="偏好类型">
          <el-select v-model="newPref.key" style="width: 100%">
            <el-option v-for="t in prefTypes" :key="t.value" :label="t.label" :value="t.value" />
          </el-select>
        </el-form-item>
        <el-form-item label="偏好值">
          <el-input v-model="newPref.value" placeholder="如：不吃辣、花生过敏" />
        </el-form-item>
      </el-form>
      <template #footer>
        <el-button @click="showAddDialog = false">取消</el-button>
        <el-button type="primary" @click="handleAdd">确定</el-button>
      </template>
    </el-dialog>
  </el-card>
</template>
```

---

### Task 13: 占位页面

**Files:**
- New: `web/src/views/AgentConfig.vue`
- New: `web/src/views/Workflow.vue`
- New: `web/src/views/Logs.vue`

- [ ] **Step 1: 创建 AgentConfig.vue**

```vue
<script setup lang="ts">
import { ref } from 'vue'

const config = ref({
  visionModel: 'Qwen-VL-Plus',
  visionStatus: '未连接',
  llmModel: 'Qwen-Plus',
  llmStatus: '未连接',
  defaultCity: '北京',
  currentSeason: '',
})

function getSeason(): string {
  const month = new Date().getMonth() + 1
  if (month >= 3 && month <= 5) return '春季'
  if (month >= 6 && month <= 8) return '夏季'
  if (month >= 9 && month <= 11) return '秋季'
  return '冬季'
}
config.value.currentSeason = getSeason()
</script>

<template>
  <div>
    <el-alert title="此页面为占位展示，后续对接后端 Agent 配置接口" type="info" show-icon :closable="false" style="margin-bottom: 16px" />

    <el-row :gutter="16">
      <el-col :span="12">
        <el-card shadow="never">
          <template #header><span style="font-weight: 600">视觉模型</span></template>
          <el-descriptions :column="1" border>
            <el-descriptions-item label="模型名称">{{ config.visionModel }}</el-descriptions-item>
            <el-descriptions-item label="连接状态">
              <el-tag type="danger">{{ config.visionStatus }}</el-tag>
            </el-descriptions-item>
          </el-descriptions>
        </el-card>
      </el-col>
      <el-col :span="12">
        <el-card shadow="never">
          <template #header><span style="font-weight: 600">语言模型</span></template>
          <el-descriptions :column="1" border>
            <el-descriptions-item label="模型名称">{{ config.llmModel }}</el-descriptions-item>
            <el-descriptions-item label="连接状态">
              <el-tag type="danger">{{ config.llmStatus }}</el-tag>
            </el-descriptions-item>
          </el-descriptions>
        </el-card>
      </el-col>
    </el-row>

    <el-card shadow="never" style="margin-top: 16px">
      <template #header><span style="font-weight: 600">环境信息</span></template>
      <el-descriptions :column="2" border>
        <el-descriptions-item label="默认城市">{{ config.defaultCity }}</el-descriptions-item>
        <el-descriptions-item label="当前季节">{{ config.currentSeason }}</el-descriptions-item>
      </el-descriptions>
    </el-card>
  </div>
</template>
```

- [ ] **Step 2: 创建 Workflow.vue**

```vue
<script setup lang="ts">
const mockData = [
  { time: '2026-05-24 15:30:00', event: 'ITEM_IN', item: '西红柿', result: '识别成功，保鲜期7天' },
  { time: '2026-05-24 15:25:00', event: 'ITEM_OUT', item: '鸡蛋', result: '匹配成功，状态更新为OUT_PENDING' },
  { time: '2026-05-24 15:20:00', event: 'ITEM_IN', item: '牛奶', result: '识别成功，保鲜期14天' },
]
</script>

<template>
  <el-card shadow="never">
    <el-alert title="此页面为占位展示，后续对接后端 Agent 处理流水接口" type="info" show-icon :closable="false" style="margin-bottom: 16px" />
    <el-table :data="mockData" stripe>
      <el-table-column prop="time" label="时间" width="200" />
      <el-table-column prop="event" label="事件类型" width="120">
        <template #default="{ row }">
          <el-tag :type="row.event === 'ITEM_IN' ? 'success' : 'warning'">{{ row.event }}</el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="item" label="食材" width="120" />
      <el-table-column prop="result" label="处理结果" />
    </el-table>
  </el-card>
</template>
```

- [ ] **Step 3: 创建 Logs.vue**

```vue
<script setup lang="ts">
const mockData = [
  { time: '2026-05-24 15:30:01', level: 'INFO', source: 'vision', message: '视觉识别成功: 西红柿 (0.92)' },
  { time: '2026-05-24 15:30:00', level: 'INFO', source: 'agent', message: 'ITEM_IN 事件处理完成' },
  { time: '2026-05-24 15:25:01', level: 'INFO', source: 'agent', message: 'ITEM_OUT 匹配成功' },
  { time: '2026-05-24 15:20:02', level: 'WARN', source: 'llm', message: 'LLM API 响应超时，使用默认保鲜期' },
  { time: '2026-05-24 15:20:00', level: 'ERROR', source: 'vision', message: '视觉 API 调用失败: connection timeout' },
]
</script>

<template>
  <el-card shadow="never">
    <el-alert title="此页面为占位展示，后续对接后端系统日志接口" type="info" show-icon :closable="false" style="margin-bottom: 16px" />
    <el-table :data="mockData" stripe>
      <el-table-column prop="time" label="时间" width="200" />
      <el-table-column prop="level" label="级别" width="100">
        <template #default="{ row }">
          <el-tag :type="row.level === 'ERROR' ? 'danger' : row.level === 'WARN' ? 'warning' : 'info'">
            {{ row.level }}
          </el-tag>
        </template>
      </el-table-column>
      <el-table-column prop="source" label="来源" width="100" />
      <el-table-column prop="message" label="消息" />
    </el-table>
  </el-card>
</template>
```

---

### Task 14: 验证与启动

- [ ] **Step 1: 启动开发服务器**

```bash
cd C:/Users/86134/PycharmProjects/luckfox_web/web && npm run dev
```

预期：Vite 启动成功，访问 http://localhost:3000 看到页面。

- [ ] **Step 2: 验证路由**

访问以下路径确认页面加载：
- http://localhost:3000/ — 库存概览
- http://localhost:3000/chat — AI 对话
- http://localhost:3000/recognize — 食材识别
- http://localhost:3000/preferences — 偏好设置
- http://localhost:3000/agent — Agent 管理
- http://localhost:3000/workflow — 工作流
- http://localhost:3000/logs — 系统日志

- [ ] **Step 3: 验证主题切换**

点击右上角主题切换按钮，确认页面在浅色/深色之间切换。
